{
  "name": "web-mcp-assistant",
  "version": "0.1.6",
  "description": "Linux Web 管理服务，用于运行 Coding Tools MCP 与 OpenAI Tunnel。",
  "author": "3169657175",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node web/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check web/server.js && node --check renderer/web-api.js && node --check renderer/app.js"
  }
}
